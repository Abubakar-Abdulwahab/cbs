@model Parkway.CBS.Module.Web.ViewModels.SearchByBINVM

@{
    Layout.Title = "Search For BIN";

    Script.Require("CBS.Popper.Script").AtFoot();
    Script.Require("CBS.Bootstrap4.Script").AtFoot();
    Script.Require("CBS.Main.Script").AtFoot();

    Style.Require("CBS.Bootstrap");
    Style.Require("CBS.Style.Main");
    Style.Require("CBS.Style.Pages");
}
    @Html.Partial("Partials\\HeaderPartial", Model.HeaderObj)

    <div class="wrapper-payment">
        <div class="top-banner alt">
            <div class="banner-bottom">
                <h3>Search For BIN</h3>
            </div>
        </div>
        <div class="container breadcrumbs">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/">Home</a></li>
                </ol>
            </nav>
        </div>
        <div class="content-payment">
            @if (Model.HasErrors)
            {
                @Html.Partial("Partials\\FlashPartial", Model.ErrorMessage)
            }
            <div class="container form-container">
                @using (Html.AppendModulePrefixToFormRouteName("BIN.Search", FormMethod.Post, new { @class = "main-form payment-form" }))
                {
                    @Html.AntiForgeryToken()
                    <label for="" class="title">Bank3D Invoice Number (BIN) <span data-toggle="tooltip" data-placement="right" data-html="true" title="<p>The Bank3D Invoice Number (BIN) is a unique number that is systematically assigned to each invoice generated on the instant revenue platform.</p><p>This number identifies each invoice and allow payers make payment for their invoices via any of the available payment channels on the platform.</p><img src='/media/images/invoice.png' alt=''>">What is this?</span></label>
                    <div class="input-group mb-3">
                        <input type="text" class="form-control mb-4 border-0" name="BIN" placeholder="Enter BIN" value="@Model.BIN" required>
                        <div class="input-group-append">
                            <button class="btn btn-orange" type="submit">Enter</button>
                        </div>
                    </div>
                    <span class="caption">Don’t have a BIN? <a href="@Url.AppendModulePrefixToRouteName("C.SelfAssessment")">Click here</a></span>
                }
            </div>
        </div>
        <div class="footer-top" @*style="background-image:@("url(" + Url.Content(Html.ThemePath(WorkContext.CurrentTheme, "/Styles/images/footer-bg.png")) + ")")"*@>

        </div>
        @Html.Partial("Partials\\FooterPartial")
    </div>
<script>
    $("#closeFlash").click(function (e) {
        $("#errorFlash").fadeOut("slow");
    });
</script>