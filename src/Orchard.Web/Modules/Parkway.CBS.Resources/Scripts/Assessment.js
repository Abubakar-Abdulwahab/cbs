$(document).ready(function () {
    // console.log('Hello!');
    // console.log(datepicker);
    $('.pickyDate').datepicker({
        format: "dd/mm/yyyy"
    });
        $('#pickyDate2').datepicker({
        format: "dd/mm/yyyy"
    });
});